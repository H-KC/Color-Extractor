import React, { Fragment } from 'react';
import Canvas from './Canvas';

const Right = () => {
  return (
    <Fragment>
      <Canvas image='images/image.png' />
    </Fragment>
  );
};

export default Right;
