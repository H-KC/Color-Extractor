import React, { Fragment } from 'react';
import Canvas from './Canvas';

const Left = () => {
  return (
    <Fragment>
      <Canvas image='images/flag.png' />
    </Fragment>
  );
};

export default Left;
